// Global Varribles
const classes = ("Ninja","Knight","Wizard","Monk");
const items = ("swords", "potions", "stickI","bomb", "compass", "dirt", "money", "ice");
let user;
let stats = {"Strength":0,"Speed":0,"Intlligence":0,"Defense":0};
let aspects = new Array; //Will add late inside the first screen
let userItems; // Displays the users items
let userName = prompt("What is you name fair traveler? "); 
// First Screen: Charater Customization
function firstScreen() {
    let spinor; //define spinor into varrible 
    while(user != null) {
        // charater customization sequence
    }
    if (user.Strength > 5){
        // Display the are strong
    } else {
        // Display they are not st
    }


}
